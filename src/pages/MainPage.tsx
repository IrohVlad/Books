import React from 'react';
import Header from '../components/Header';
import BookGrig from '../components/BookGrig';

const MainPage = () => {
    return (
        <>
        <Header/>
        <main>
            <BookGrig/>
        </main>
        </>
    );
};

export default MainPage;